<local:BaseModellerDialog x:Class="Make3D.Dialogs.Propeller"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Make3D.Dialogs"
        mc:Ignorable="d"
        Title="Propeller" Height="450" Width="800" Loaded="Window_Loaded" WindowStartupLocation="CenterOwner">
    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Grid.Row="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Label Width="100">No. of Blades</Label>
                <Slider Value="{Binding NumberOfBlades}" Minimum="2" Maximum="8" Width="200" />
                <TextBox Text="{Binding NumberOfBlades}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Label Width="100">Blade Length</Label>
                <Slider Value="{Binding BladeLength}" Minimum="10" Maximum="100" Width="200" />
                <TextBox Text="{Binding BladeLength}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal">
                <Label Width="100">Root Offset</Label>
                <Slider Value="{Binding RootOffset}" Minimum="0" Maximum="100" Width="200" />
                <TextBox Text="{Binding RootOffset}" Width="50" />
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal">
                <Label Width="100">Mid Offset</Label>
                <Slider Value="{Binding MidOffset}" Minimum="5" Maximum="20" Width="200" />
                <TextBox Text="{Binding MidOffset}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="5" Orientation="Horizontal">
                <Label Width="100">Balde Root</Label>
                <Slider Value="{Binding BladeRoot}" Minimum="5" Maximum="20" Width="200" />
                <TextBox Text="{Binding BladeRoot}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="6" Orientation="Horizontal">
                <Label Width="100">Blade Mid</Label>
                <Slider Value="{Binding BladeMid}" Minimum="5" Maximum="20" Width="200" />
                <TextBox Text="{Binding BladeMid}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="7" Orientation="Horizontal">
                <Label Width="100">Blade Tip</Label>
                <Slider Value="{Binding BladeTip}" Minimum="5" Maximum="20" Width="200" />
                <TextBox Text="{Binding BladeTip}" Width="50" />
            </StackPanel>
            <StackPanel Grid.Row="8" Orientation="Horizontal">
                <Label Width="100">Blade Angle</Label>
                <Slider Value="{Binding BladeAngle}" Minimum="0" Maximum="90" Width="200" />
                <TextBox Text="{Binding BladeAngle}" Width="50" />
            </StackPanel>

            <StackPanel Grid.Row="9" Orientation="Horizontal">
                <Label Width="100">Blade Profile</Label>
                <ComboBox Width="100"  Margin="0,0,10,0" VerticalAlignment="Center" ItemsSource="{Binding AirfoilGroups}" SelectedItem="{Binding RootGroup}" />
                <ComboBox Width="100"  Margin="0,0,10,0" ItemsSource="{Binding AirfoilNames}" VerticalAlignment="Center" SelectedItem="{Binding SelectedRootAirfoil}" />
            </StackPanel>
            <StackPanel Grid.Row="10" Orientation="Horizontal">
                <Label Width="100">Hub Type</Label>
                <RadioButton GroupName="HubGroup" Margin="8,0,0,0" VerticalAlignment="Center">Flat</RadioButton>
                <RadioButton GroupName="HubGroup" Margin="8,0,0,0" VerticalAlignment="Center">Domed</RadioButton>
                <RadioButton GroupName="HubGroup" Margin="8,0,0,0" VerticalAlignment="Center">Cone</RadioButton>
            </StackPanel>
        </Grid>
        <Border Grid.Row ="0" Grid.Column="1" BorderThickness="4" BorderBrush="Blue">
            <Viewport3D Name="viewport3D1"   MouseMove="Viewport_MouseMove"
              MouseDown="Viewport_MouseDown" MouseWheel="Viewport_MouseWheel">

                <Viewport3D.Camera>
                    <PerspectiveCamera x:Name = "camMain" Position = "{Binding CameraPosition}" LookDirection = "{Binding LookDirection}">
                    </PerspectiveCamera>
                </Viewport3D.Camera>

                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <Model3DGroup>
                            <AmbientLight Color="#FF808080" />
                            <DirectionalLight x:Name = "dirLightMain" Direction = "-1,-1,-1" Color="White">
                            </DirectionalLight>
                        </Model3DGroup>
                    </ModelVisual3D.Content>
                </ModelVisual3D>

                <ModelVisual3D x:Name = "MyModel">
                    <ModelVisual3D.Content>
                        <Model3DGroup x:Name="MyModelGroup">
                        </Model3DGroup>
                    </ModelVisual3D.Content>
                </ModelVisual3D>
            </Viewport3D>
        </Border>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid Grid.Column="0" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <CheckBox Content="Floor" Grid.Column="0" IsChecked="{Binding ShowFloor}" />
            <CheckBox Content="Axies" Grid.Column="1" IsChecked="{Binding ShowAxies}" />
        </Grid>

        <Grid Grid.Column="1" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Cancel" Click="Cancel_Click" Margin="2,2,2,2" />
            <Button Grid.Column="2" Content="Ok" Click="Ok_Click"  Margin="2,2,2,2" />
        </Grid>
    </Grid>
</local:BaseModellerDialog>