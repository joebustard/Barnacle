<UserControl x:Class="Make3D.Views.DefaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

             xmlns:Views="clr-namespace:Make3D.Views"
          xmlns:ViewModels="clr-namespace:Make3D.ViewModels"
             xmlns:local="clr-namespace:Make3D.Views"
             xmlns:Custom="clr-namespace:System.Windows.Controls.Ribbon;assembly=System.Windows.Controls.Ribbon"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:DefaultViewModel />
    </UserControl.DataContext>
    <Grid ScrollViewer.CanContentScroll="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="140" />
            <RowDefinition />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <Custom:Ribbon  x:Name="MainRibbon" Grid.Row="0"  MinHeight="100" SelectionChanged="MainRibbon_SelectionChanged">
            <Custom:Ribbon.QuickAccessToolBar>
                <Custom:RibbonQuickAccessToolBar>
                    <Custom:RibbonButton x:Name="New" SmallImageSource="/images/buttons/new_file.png" Command="{Binding NewCommand}" />
                    <Custom:RibbonButton x:Name="Open" SmallImageSource="/images/buttons/human_document_open.png" Command="{Binding OpenCommand}" />
                    <Custom:RibbonButton x:Name="SaveButton" SmallImageSource="/images/buttons/document_save_as.png" Command="{Binding SaveCommand}" />
                    <Custom:RibbonButton x:Name="SaveItAs" SmallImageSource="/images/buttons/document_save_as.png" Command="{Binding SaveAsCommand}" />
                    <Custom:RibbonButton x:Name="Undo" SmallImageSource="/images/buttons/edit_undo.png" Command="{Binding UndoCommand}" />
                    <Custom:RibbonButton x:Name="Redo" SmallImageSource="/images/buttons/edit_redo.png" Command="{Binding RedoCommand}" />
                </Custom:RibbonQuickAccessToolBar>
            </Custom:Ribbon.QuickAccessToolBar>
            <Custom:Ribbon.ApplicationMenu>
                <Custom:RibbonApplicationMenu KeyTip="f">
                    <Custom:RibbonApplicationMenu.AuxiliaryPaneContent>
                        <Grid VerticalAlignment="Stretch"
							HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0">
                                <TextBlock Text="Recent Items"
									FontWeight="Bold"
									Margin="5" />
                            </Border>
                            <ItemsControl Grid.Row="1" ItemsSource="{Binding RecentFilesList}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Custom:RibbonApplicationMenuItem Header="{Binding Name}"
                                             Command="{Binding DataContext.OpenRecentFileCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                         CommandParameter="{Binding Path}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Custom:RibbonApplicationMenu.AuxiliaryPaneContent>
                    <Custom:RibbonApplicationMenuItem x:Name="New1"  Header="New" ImageSource="/images/buttons/new_file.png" Command="{Binding NewCommand}" />
                    <Custom:RibbonApplicationMenuItem x:Name="Open1" Header="Open" ImageSource="/images/buttons/human_document_open.png" Command="{Binding OpenCommand}" />
                    <Custom:RibbonApplicationMenuItem x:Name="Save1" Header="Save" ImageSource="/images/buttons/document_save_as.png" Command="{Binding SaveCommand}" />
                    <Custom:RibbonApplicationMenuItem x:Name="Save2" Header="Save As" ImageSource="/images/buttons/document_save_as.png" Command="{Binding SaveAsCommand}" />
                    <Custom:RibbonApplicationMenuItem x:Name="Insert" Header="Insert" ImageSource="/images/buttons/human_document_open.png" Command="{Binding InsertCommand}" />
                    <!--   <Custom:RibbonApplicationMenuItem Header="Print" ImageSource="/images/buttons/Printer.png" Command="{Binding PrintCommand}" />
                    <Custom:RibbonApplicationMenuItem Header="Print Preview" ImageSource="/images/buttons/Printer.png" Command="{Binding ViewCommand}" CommandParameter="PrintPreview" />
-->
                    <Custom:RibbonApplicationMenuItem Header="Exit" ImageSource="/images/buttons/exit.png" Command="{Binding ExitCommand}" />
                </Custom:RibbonApplicationMenu>
            </Custom:Ribbon.ApplicationMenu>

            <Custom:RibbonTab Name="HomeTab1" Header="Home">
                <Custom:RibbonGroup Name="ClipboardGroup" Header="Clipboard" LargeImageSource="/Make3D;component/Images/Buttons/clipboard.png">
                    <Custom:RibbonButton x:Name="CopyButton" Label="Cut" Command="{Binding CutCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" />
                    <Custom:RibbonButton x:Name="CutButton" Label="Copy" Command="{Binding CopyCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />
                    <Custom:RibbonButton x:Name="PasteButton" Label="Paste" Command="{Binding PasteCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/paste.png" />

                    <Custom:RibbonButton x:Name="PasteMultiButton" Label="Multi Paste" Command="{Binding MultiPasteCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/paste.png" />

                    <Custom:RibbonButton x:Name="PasteCircularButton" Label="Circular Paste" Command="{Binding CircularPasteCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/paste.png" />
                </Custom:RibbonGroup>

                <Custom:RibbonGroup Name="SelectionGroup" Header="Selection" LargeImageSource="/Make3D;component/Images/Buttons/SelectAll.png">

                    <Custom:RibbonButton x:Name="SelectFirstButton" Label="First" Command="{Binding SelectCommand}" CommandParameter="First" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectAll.png" ToolTip="Select First" />

                    <Custom:RibbonButton x:Name="SelectPrevButton" Label="Previous" Command="{Binding SelectCommand}" CommandParameter="Previous" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectAll.png" ToolTip="Select Previous" />

                    <Custom:RibbonButton x:Name="SelectNextButton" Label="Next" Command="{Binding SelectCommand}" CommandParameter="Next" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectAll.png" ToolTip="Select Next" />

                    <Custom:RibbonButton x:Name="SelectLastButton" Label="Last" Command="{Binding SelectCommand}" CommandParameter="Last" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectAll.png" ToolTip="Select Last" />

                    <Custom:RibbonButton x:Name="SelectAllButton" Label="All" Command="{Binding SelectCommand}" CommandParameter="All" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectAll.png" ToolTip="Select All" />
                    <Custom:RibbonButton x:Name="SelectNoneButton" Label="Clear" Command="{Binding SelectCommand}" CommandParameter="Clear" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/SelectClear.png" ToolTip="Select All" />
                </Custom:RibbonGroup>

                <!--
                <Custom:RibbonGroup Name="TextGroup" Header="Text" LargeImageSource="/Make3D;component/Images/Buttons/BigText.png">
                    <ComboBox x:Name="FontFamilySelector"
          ItemsSource="{Binding SystemFonts}"
          HorizontalAlignment="Left"
          Margin="0,0,0,0"
          VerticalAlignment="Top" Text="{Binding SelectedFont}">
                        <ComboBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel Width="150" />
                            </ItemsPanelTemplate>
                        </ComboBox.ItemsPanel>
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="24">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Margin="3"
                           Text="{Binding}" FontFamily="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <StackPanel Orientation="Horizontal">
                        <Custom:RibbonToggleButton  IsChecked="{Binding BoldChecked}" Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Label="B" FontWeight="Bold" />
                        <Custom:RibbonToggleButton  IsChecked="{Binding ItalicChecked}" Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Label="I" FontStyle="Italic" FontWeight="Bold" />
                        <Custom:RibbonToggleButton  IsChecked="{Binding UnderlineChecked}" Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Label="U"  FontWeight="Bold" />
                        <ComboBox Width="45" Height="20" Text="{Binding FontSize}">
                            <ListBoxItem Content="8" />
                            <ListBoxItem Content="10" />
                            <ListBoxItem Content="12" />
                            <ListBoxItem Content="14" />
                            <ListBoxItem Content="16" />
                            <ListBoxItem Content="22" />
                            <ListBoxItem Content="32" />
                            <ListBoxItem Content="72" />
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Custom:RibbonRadioButton Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding TextAlignmentCommand}" CommandParameter="L" IsChecked="{Binding LeftTextAlignment}" SmallImageSource="/Make3D;component/Images/Buttons/Left.png" />
                        <Custom:RibbonRadioButton Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding TextAlignmentCommand}" CommandParameter="C" IsChecked="{Binding CenterTextAlignment}" SmallImageSource="/Make3D;component/Images/Buttons/Center.png" />
                        <Custom:RibbonRadioButton Width="20" Height="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Command="{Binding TextAlignmentCommand}" CommandParameter="R" IsChecked="{Binding RightTextAlignment}" SmallImageSource="/Make3D;component/Images/Buttons/Right.png" />
                    </StackPanel>
                </Custom:RibbonGroup>
-->
                <Custom:RibbonGroup Name="ProjectGroup" Header="Project" LargeImageSource="/Make3D;component/Images/Buttons/objects.png">
                    <Custom:RibbonButton x:Name="ProjectSettings" Label="Settings" Command="{Binding SettingsCommand}" CommandParameter="Settings" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" />
                    <Custom:RibbonMenuButton x:Name="Import" Label="Import">
                        <Custom:RibbonButton x:Name="ImportOBJ" Label="OBJ" Command="{Binding ImportCommand}" CommandParameter="OBJ" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" />
                        <Custom:RibbonButton x:Name="ImportSTL" Label="STL" Command="{Binding ImportCommand}" CommandParameter="STL" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Export" Label="Export">
                        <Custom:RibbonButton x:Name="STL" Label="STL" Command="{Binding ExportCommand}" CommandParameter="STL" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" />
                        <Custom:RibbonButton x:Name="STLParts" Label="STL Parts" Command="{Binding ExportPartsCommand}" CommandParameter="STLParts" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" ToolTip="Export each part into its own STL file" />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Slice" Label="Slice">
                        <Custom:RibbonButton x:Name="Slicer" Label="Sl3cer" Command="{Binding SliceCommand}" CommandParameter="SliceAll" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" ToolTip="Export to stl, slice ( and copy to sdcard if present)" />
                    </Custom:RibbonMenuButton>
                </Custom:RibbonGroup>
                <Custom:RibbonGroup Name="DataGroup" Header="Objects" LargeImageSource="/Make3D;component/Images/Buttons/objects.png">
                    <Custom:RibbonMenuButton x:Name="Flip" Label="Flip">
                        <Custom:RibbonButton x:Name="HorizontalFlip" Label="Horizontal" Command="{Binding FlipCommand}" CommandParameter="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" ToolTip="Flip objects vertices horizontally." />
                        <Custom:RibbonButton x:Name="VerticalFlip" Label="Vertical" Command="{Binding FlipCommand}" CommandParameter="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their right edges match." />
                        <Custom:RibbonButton x:Name="InsideFlip" Label="Inside out" Command="{Binding FlipCommand}" CommandParameter="Inside" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their right edges match." />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Alignment" Label="Align">
                        <Custom:RibbonButton x:Name="LeftAlignment" Label="Left" Command="{Binding AlignCommand}" CommandParameter="Left" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" ToolTip="Align objects so their left edges match the first selected." />
                        <Custom:RibbonButton x:Name="RightAlignment" Label="Right" Command="{Binding AlignCommand}" CommandParameter="Right" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their right edges match the first selected." />
                        <Custom:RibbonButton x:Name="TopAlignment" Label="Top" Command="{Binding AlignCommand}" CommandParameter="Top" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/cut.png" ToolTip="Align objects so their top edges match the first selected." />
                        <Custom:RibbonButton x:Name="BottomAlignment" Label="Bottom" Command="{Binding AlignCommand}" CommandParameter="Bottom" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their bottom edges match the first selected." />

                        <Custom:RibbonButton x:Name="FrontAlignment" Label="Front" Command="{Binding AlignCommand}" CommandParameter="Front" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their front edges match the first selected." />
                        <Custom:RibbonButton x:Name="BackAlignment" Label="Back" Command="{Binding AlignCommand}" CommandParameter="Back" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so their back edges match the first selected." />

                        <Custom:RibbonButton x:Name="FloorAlignment" Label="Floor" Command="{Binding AlignCommand}" CommandParameter="Floor" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all on the floor." />
                        <Custom:RibbonButton x:Name="CentreAlignment" Label="Center" Command="{Binding AlignCommand}" CommandParameter="Centre" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered on the first selected." />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Stack" Label="Stack">
                        <Custom:RibbonButton x:Name="StackLeftAlignment" Label="Stack Left" Command="{Binding AlignCommand}" CommandParameter="StackLeft" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked To the left of the first selected." />
                        <Custom:RibbonButton x:Name="StackRightAlignment" Label="Stack Right" Command="{Binding AlignCommand}" CommandParameter="StackRight" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked To the right of the first selected." />

                        <Custom:RibbonButton x:Name="StackVAlignment" Label="Stack Above" Command="{Binding AlignCommand}" CommandParameter="StackY" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked above the first selected." />
                        <Custom:RibbonButton x:Name="StackBelowAlignment" Label="Stack Below" Command="{Binding AlignCommand}" CommandParameter="StackBottom" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked below the first selected." />
                        <Custom:RibbonButton x:Name="StackZAlignment" Label="Stack In Front" Command="{Binding AlignCommand}" CommandParameter="StackZ" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked To the front of the first selected." />

                        <Custom:RibbonButton x:Name="StackBehindAlignment" Label="Stack Behind" Command="{Binding AlignCommand}" CommandParameter="StackBehind" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Align objects so they are all centered and stacked to the rear of the first selected." />
                    </Custom:RibbonMenuButton>

                    <Custom:RibbonMenuButton x:Name="Distibution" Label="Distribution">
                        <Custom:RibbonButton x:Name="Optimum" Label="Optimum" Command="{Binding DistributeCommand}" CommandParameter="Optimum" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Distribute all items to minimise printing footprint" />
                        <Custom:RibbonButton x:Name="VEvenAlignment" Label="Vertical Selection" Command="{Binding AlignCommand}" CommandParameter="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Distribute selection evenly between the highest and lowest" />
                        <Custom:RibbonButton x:Name="HEvenAlignment" Label="Horizontal Selection" Command="{Binding AlignCommand}" CommandParameter="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Distribute selection evenly between the left most and right most" />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Size" Label="Size">
                        <Custom:RibbonButton x:Name="SameLength" Label="Same Length" Command="{Binding SizeCommand}" CommandParameter="SameLength" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Make all selected objects the same length as the first selected one" />
                        <Custom:RibbonButton x:Name="SameWidth" Label="Same Width" Command="{Binding SizeCommand}" CommandParameter="SameWidth" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Make all selected objects the same width as the first selected one" />
                        <Custom:RibbonButton x:Name="SameHeight" Label="Same Height" Command="{Binding SizeCommand}" CommandParameter="SameHeight" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Make all selected objects the same height as the first selected one" />
                        <Custom:RibbonButton x:Name="SameBoth" Label="All Same" Command="{Binding SizeCommand}" CommandParameter="SameAll" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Make all selected objects the same size as the first selected one" />
                        <Custom:RibbonSeparator />

                        <Custom:RibbonButton x:Name="DecimateButton" Label="Decimate Faces" Command="{Binding SizeCommand}" CommandParameter="Decimate" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" ToolTip="Reduce the number of faces in the selected object" />
                    </Custom:RibbonMenuButton>
                    <Custom:RibbonMenuButton x:Name="Group" Label="Group">
                        <Custom:RibbonButton x:Name="GroupUnion" Label="Group Union" Command="{Binding GroupCommand}" CommandParameter="GroupUnion" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />

                        <Custom:RibbonButton x:Name="GroupDifference" Label="Group Difference" Command="{Binding GroupCommand}" CommandParameter="GroupDifference" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />

                        <Custom:RibbonButton x:Name="GroupIntersection" Label="Group Intersection" Command="{Binding GroupCommand}" CommandParameter="GroupIntersection" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />

                        <Custom:RibbonButton x:Name="UngroupSelected" Label="Ungroup" Command="{Binding GroupCommand}" CommandParameter="Ungroup" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />
                        <Custom:RibbonSeparator />
                        <Custom:RibbonButton x:Name="SaveSelected" Label="Group To Mesh" Command="{Binding GroupCommand}" CommandParameter="Mesh" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/copy.png" />
                    </Custom:RibbonMenuButton>
                </Custom:RibbonGroup>
                <Custom:RibbonGroup Name="ToolGroup" Header="Tools">
                    <Custom:RibbonButton x:Name="IrregularButton" Label="Platelet"   IsEnabled="{Binding IrregularEnabled}" Command="{Binding IrregularCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="LinearButton" Label="Linear" Command="{Binding LinearCommand}" IsEnabled="{Binding LinearEnabled}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/Vase.png"
  ToolTip="Create an object by linear lofting" />
                    <Custom:RibbonButton x:Name="DoughButton" Label="DoughNut" Command="{Binding DoughnutCommand}" IsEnabled="{Binding DoughnutEnabled}"  HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="FuselageButton" Label="Fuselage" Command="{Binding FuselageCommand}" IsEnabled="{Binding FuselageEnabled}"  HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="ShapeLoftButton" Label="Two Shape" Command="{Binding TwoShapeCommand}" IsEnabled="{Binding TwoShapeEnabled}"  HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="SpurGearButton" Label="Spur Gear" Command="{Binding SpurGearCommand}" IsEnabled="{Binding SpurGearEnabled}"  HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                </Custom:RibbonGroup>
            </Custom:RibbonTab>
            <Custom:RibbonTab Name="ViewTab1" Header="View">
                <Custom:RibbonGroup Name="ZoomGroup" Header="Zoom">
                    <Custom:RibbonButton x:Name="ZoomOutButton" Label="Zoom Out" Command="{Binding ZoomOutCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="ZoomInButton" Label="Zoom In" Command="{Binding ZoomInCommand}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                    <Custom:RibbonButton x:Name="Zoom100Button" Label="100%" Command="{Binding Zoom100Command}" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                </Custom:RibbonGroup>

                <Custom:RibbonGroup Name="ShowGroup" Header="Show">
                    <Custom:RibbonCheckBox x:Name="ShowFloor" Label="Floor" IsChecked="{Binding ShowFloorChecked}" />
                    <Custom:RibbonCheckBox x:Name="ShowAxis" Label="Axis" IsChecked="{Binding ShowAxiesChecked}" />
                </Custom:RibbonGroup>
                <Custom:RibbonGroup Name="OptionGroup" Header="Options">
                </Custom:RibbonGroup>
                <Custom:RibbonGroup Name="HelpGroup" Header="Help" LargeImageSource="/Make3D;component/Images/Buttons/question1.png">
                    <Custom:RibbonButton x:Name="AboutButton" Label="About" Command="{Binding ViewCommand}" CommandParameter="About" HorizontalAlignment="Stretch" VerticalAlignment="Top" SmallImageSource="/Make3D;component/Images/Buttons/question1.png" />
                </Custom:RibbonGroup>
            </Custom:RibbonTab>
        </Custom:Ribbon>
        <Grid x:Name="MyGrid" Grid.Row="1" SizeChanged="Grid_SizeChanged">
            <DockPanel LastChildFill="True">
                <ScrollViewer>
                    <StackPanel Width="75">
                        <local:CameraPalette HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="75" Visibility="{Binding CameraPaletteVisible}" />
                        <local:ObjectPalette HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="75" Visibility="{Binding ToolPaletteVisible}" />
                    </StackPanel>
                </ScrollViewer>
                <Views:ObjectPropertiesView HorizontalAlignment="Right" VerticalAlignment="Stretch" />
                <ScrollViewer x:Name="Scroller" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <ContentControl x:Name="SubViewControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Content="{Binding SubView}" />
                </ScrollViewer>
            </DockPanel>
        </Grid>
        <StatusBar Grid.Row="2">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="400" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Content="{Binding StatusBlockText1}" Grid.Column="0" />
            <Separator Grid.Column="1" />
            <StatusBarItem Content="{Binding StatusBlockText2}" Grid.Column="2" />
            <Separator Grid.Column="3" />
            <StatusBarItem Content="{Binding StatusBlockText3}" Grid.Column="4" HorizontalAlignment="Right" />

            <StatusBarItem Width="120" />
        </StatusBar>
    </Grid>
</UserControl>