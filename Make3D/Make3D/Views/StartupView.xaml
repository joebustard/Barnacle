<UserControl x:Class="Make3D.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

                          xmlns:Views="clr-namespace:Make3D.Views"
          xmlns:ViewModels="clr-namespace:Make3D.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:StartupViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="24*" />
            <ColumnDefinition Width="565*" />
            <ColumnDefinition Width="211*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" Content="{Binding AppIdentity}" FontSize="18" Margin="321,0,100,0" Width="144" />
        <Button Grid.Row="3" Grid.Column="2" HorizontalAlignment="Center" MaxHeight="100" MaxWidth="200" FontSize="18" Command="{Binding NewProjectCommand}"  Width="120">New Project</Button>
        <Button Grid.Row="6" Grid.Column="2" HorizontalAlignment="Center" MaxHeight="100" MaxWidth="200" FontSize="18" Command="{Binding OpenProjectCommand}"  Width="120" Visibility="{Binding OpenVisible}">Open Project</Button>
        <Label Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center"  VerticalAlignment="Center" FontSize="18" Margin="205,11,230,11" Height="34" Width="130">Recent Projects</Label>
        <ListView Grid.Row="2" Grid.Column="1" Grid.RowSpan="5" Margin="4,4,4,4" BorderThickness="4" ItemsSource="{Binding RecentProjects}" SelectedItem="{Binding SelectedProject}">

            <ListView.ItemTemplate>

                <DataTemplate>
                    <Border BorderThickness="0,0,0,2">
                        <StackPanel Orientation="Horizontal">
                            <Image Width="32" Height="32" Source="/Make3D;component/Images/Buttons/Open.png" />
                            <Label FontSize="16" Width="300" Content="{Binding Title}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>