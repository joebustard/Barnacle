<UserControl x:Class="Make3D.Views.ScriptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Make3D"
                            xmlns:Views="clr-namespace:Make3D.Views"
          xmlns:ViewModels="clr-namespace:Make3D.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="ScriptView_Loaded">
    <UserControl.DataContext>
        <ViewModels:ScriptViewModel />
    </UserControl.DataContext>
    <Grid>
        <TabControl>
            <TabItem Header="Script">
                <Grid x:Name="ScriptGrid">

                    <Grid.RowDefinitions>
                        <RowDefinition  Height="25" />
                        <RowDefinition />
                        <RowDefinition  Height="25" />
                        <RowDefinition  Height="25" />
                        <RowDefinition  Height="25" />
                        <RowDefinition  Height="25" />
                        <RowDefinition  Height="25" />
                    </Grid.RowDefinitions>
                    <UniformGrid Grid.Row="0"  Columns="3" Rows="1">
                        <Button x:Name="NewScript" Margin="2,2,2,2" Content="New" Command="{Binding ScriptCommand}" CommandParameter="New" />
                        <Button x:Name="LoadScript" Margin="2,2,2,2" Content="Load" Command="{Binding ScriptCommand}" CommandParameter="Load" />
                        <Button x:Name="SaveScript" Margin="2,2,2,2" Content="Save" Command="{Binding ScriptCommand}" CommandParameter="Save" />
                    </UniformGrid>
                    <RichTextBox Grid.Row="1" x:Name="ScriptBox" Block.LineHeight="2" TextChanged="ScriptBox_TextChanged" PreviewKeyDown="ScriptBox_PreviewKeyDown" HorizontalAlignment="Stretch" />
                    <Button Grid.Row="2" Content="Run" Click="RunClicked" />
                    <TextBox x:Name="ResultsBox" Grid.Row="3" Grid.RowSpan="3" Background="Black" Foreground="AntiqueWhite" />
                    <Button Grid.Row="6" Content="Clear" Command="{Binding ScriptCommand}" CommandParameter="Clear" />
                </Grid>
            </TabItem>
            <TabItem Header="3D Output">
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>