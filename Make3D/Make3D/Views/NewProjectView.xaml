<UserControl x:Class="Make3D.Views.NewProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Make3D.Views"
             xmlns:ViewModels="clr-namespace:Make3D.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:NewProjectViewModel />
    </UserControl.DataContext>
    <Grid HorizontalAlignment="Center"  MaxWidth="800" MaxHeight="450">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="151*" />
            <ColumnDefinition Width="169*" />
            <ColumnDefinition Width="155*" />
            <ColumnDefinition Width="165*" />
            <ColumnDefinition Width="80*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="35" />
            <RowDefinition />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />

            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Label Grid.Column="1" VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Left" Margin="0,0.6,0,0.8" Grid.Row="1">Project Name</Label>
        <TextBox Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Stretch"   FontSize="16" Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"  Margin="0,3.6,0,2.8" Grid.ColumnSpan="2" />

        <Label Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Left" Margin="0,1.2">Template</Label>

        <ComboBox Grid.Row="2" x:Name="TemplateBox" Grid.Column="2" ItemsSource="{Binding TemplateNames}" Margin="0,0.2" Grid.ColumnSpan="2" SelectedItem="{Binding SelectedTemplate}" />
        <Label Grid.Row="4" Grid.Column="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" FontSize="18" Margin="117.8,0.4,105.169,201.4" Grid.RowSpan="2">Description</Label>
        <TextBox x:Name="DescriptionBox" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="4" AcceptsReturn="True" IsReadOnly="True" BorderThickness="4" Margin="4,4.2,4,3.4" Text="{Binding SelectedDescription}">
        </TextBox>

        <Grid Grid.Row="7" Grid.Column="3" Grid.ColumnSpan="3" Margin="0,0.2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Back" Command="{Binding BackCommand}" Margin="4,4,4,4" />
            <Button Grid.Column="2" Content="Create" Command="{Binding CreateCommand}" IsEnabled="{Binding OKEnabled}" Margin="4,4,4,4" />
        </Grid>
    </Grid>
</UserControl>