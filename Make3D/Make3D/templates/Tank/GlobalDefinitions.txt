   // Some global parameters for a typical tank
   // Add your own specific ones here.
   // 
   // Include in all the tank scripts you create in project
   // so they are all using the same values for these
   // Assume tank is of the floor, facing to the right
   String projectpath = "C:\Users\Your Name\Documents\Barnacle\TankName" ;

   // String thats used to generate the display label
   string DisplayLabel="Tank Name";
   
   // scaling things may reduce them below the minimum size that we can practically print
   // Set a limit. Set to 0 if you dont want a limit at all.
   Double minimumDimension = 1;

   // default size for any surface or wheel nuts
   Double nutsize = 0.75 ;
   Double smallnut = 0.5;

